import { Container } from 'inversify';
import { TYPES } from './types';

/**
 * This file is a placeholder for one that is code-generated from swagger.
 */
function bindControllers(container: Container): void {
}

export { bindControllers }
